AuctionHouse.LotsController = Ember.ArrayController.extend({
  needs: ['items'],

  // var itemsList = this.get('controllers.items.model')

    // var lotsArray = this.get('model');
    // var itemsArray = [];
    // lotsArray.forEach(function(lot) {
    //
    //   itemsArray.push(lot.items)
    // });
});
