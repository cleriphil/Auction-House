<h1> Lot title: {{title}}</h1>

{{#if isEditing}}

  <form>
    <label for="title">Title</label>
    {{input id="title" class="form-control" value=title}}

    <label for="description">Description</label>
    {{textarea id="description" class="form-control" value=description}}

    <label for="date">Date</label>
    {{input id="date" class="form-control" value=date}}
  <br>

      <button class="btn btn-primary" {{action 'save'}}>Edit lot</button>
  </form>
{{else}}
  <h3>Description: {{description}}</h3>

  <h3>Auction date: {{date}}</h3>

  <h2>Items:</h2>

  <ul class="lotlist">

    {{#each items}}
     <li class="flat">{{#link-to 'item' this.id}}<img src={{image}} class="img-thumbnail">{{/link-to}} {{title}} {{description}}</li>
    {{/each}}

  </ul>
  <p>{{#link-to 'new-item' this.id classNames="btn btn-primary"}}Add New Item to this lot{{/link-to}}</p>
   {{outlet}}


  <button class="btn btn-primary" {{action 'edit'}}>Edit lot</button>
  <button class="btn btn-danger" {{action 'delete'}}>Delete lot</button>
{{/if}}
